source $DOTFILES_DIR/vars

#load alias files
if [ -d $dotfiles_aliasdir ]; then
    for f in $dotfiles_aliasdir/*; do source $f; done
fi

#add dotfile cli
dotfiles() {
    if [[ $@ == "update" ]]; then
        old_dir=$PWD
        cd $dir && chmod +x update.sh && ./update.sh ; cd $old_dir
    fi
}